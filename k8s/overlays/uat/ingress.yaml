apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: candidate-api
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: uat-strip-uat-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /uat
            pathType: Prefix
            backend:
              service:
                name: candidate-api
                port:
                  number: 80
